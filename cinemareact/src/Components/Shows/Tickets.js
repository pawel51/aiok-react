import React from 'react';
import {Button} from "react-bootstrap";

const Tickets = () => {
    return (
        <div>
            <Button variant={"primary"}>HelloButton</Button>
        </div>
    );
};

export default Tickets;